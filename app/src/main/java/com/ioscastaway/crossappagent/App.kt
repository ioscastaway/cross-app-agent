package com.ioscastaway.crossappagent

import android.app.Application

/**
 * Nothing to see here yet. Exists so later phases (overlay bubble, voice entry point,
 * macro cache) have a process-wide home without a refactor.
 */
class App : Application()
